// Описывает "Полносвязанный" трехслойный персептрон
// Слои : input, hidde, output
class ThreeLayerPerceptron {

    // Создает персептрон с заданным кол-вом элементов входного, 
    // скрытого и выходного слоев
    constructor(inputAmount, hiddenAmount, outputAmount) {
        this.input_length = inputAmount;
        this.hidden_length = hiddenAmount;
        this.output_length = outputAmount;
    }

}
